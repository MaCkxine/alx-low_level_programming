#include <stdio.h>
#include "main.h"

/**
 * main - swap values of integer
 * @a: swap integer
 * @b: swap integer
 * Return 0
 */
void swap_int(int *a, int *b);

	int x;
	{
		x = *a;
		*a = *b
		*b = x;
	}
